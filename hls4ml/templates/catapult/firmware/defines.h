#ifndef DEFINES_H_
#define DEFINES_H_

#include "ac_int.h"
#include "ac_fixed.h"
#include "ac_channel.h"
#include "nnet_utils/nnet_types.h"
#include <cstddef>
#include <cstdio>

// Define location of weight files to work with SCVerify which assumes that relative paths
// are relative to the directory containing the Catapult project, i.e. 'my-ccs-test'
#define WEIGHTS_DIR "firmware/weights"

//hls-fpga-machine-learning insert numbers

//hls-fpga-machine-learning insert layer-precision

#endif
